---
paths:
  - ~/.claude/skills/
---
# skills — standing

The cross-session skills that run fund's daily dev cycle — `morning-standup`,
`eod-digest`, `owning-a-lane`, `get-aligned`, `split-the-plan`,
`setup-dev-cycle` and peers — live outside this repo in `~/.claude/skills/`
(git, private origin `benjaminematton/claude-skills`; commit skill edits
there, direct to `main`). No spec/design docs in that repo — its `.gitignore`
refuses `docs/`; specs and plans for skill work live here in
`docs/superpowers/`. A lane whose work lands in the skills repo still ships
its journal entry to THIS file, as a one-file PR to fund. Cross-session
doctrine: ownership map at `~/.claude/align/fund/map.md`; decisions only in
`~/.claude/align/fund/decisions.md`, written only by Benjamin.

# Journal
